- add hashes to JS & CSS files

- content security policy (hashes for all the scripts)
  - no inline scripts
  - make local copy of every CDN thing

- store user token in localStorage, not cookies
  OR
- CSRF

- one-time code

- OTP ratelimit: burst limit = 5, regen 1 per minute
