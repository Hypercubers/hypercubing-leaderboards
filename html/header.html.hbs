<header>
    <script src="/js/redirect-here.js" type="text/javascript" ></script>
    {{#if active_user.name}}
        Signed in as <a href="/solver?id={{active_user.id}}">{{active_user.name}}</a>;
        <a href="/signed-out" class="redirect-here">Sign out</a>
    {{else}}
        <a href="/sign-in" class="redirect-here">Sign in</a>
    {{/if}}
    <hr/>
</header>
